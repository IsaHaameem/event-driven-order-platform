server:
  port: 8083

spring:
  application:
    name: email-service

  datasource:
    url: jdbc:postgresql://postgres-email:5432/email_db
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  kafka:
    bootstrap-servers: kafka:19092
    consumer:
      group-id: email-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.orderplatform.emailservice.event
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.orderplatform.emailservice.event.OrderCreatedEventV1
    producer:
      # Not called directly by our code — @RetryableTopic uses this KafkaTemplate
      # internally to forward failed messages to the retry/DLT topics.
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      properties:
        enable.idempotence: true
        linger.ms: 5
        request.timeout.ms: 30000
        delivery.timeout.ms: 60000

app:
  email:
    simulated-failure-rate: 0.2
    always-fail-for-customer-id: ${APP_EMAIL_ALWAYS_FAIL_FOR_CUSTOMER_ID:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html